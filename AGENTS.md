# AGENTS.md

## Purpose
This repository hosts the profile README and GitHub workflow automation for user `LHY0125`.

## Repository Structure
- `/README.md`: Main profile content and badges/widgets.
- `/.github/workflows/waka-box.yml`: Workflow related to WakaTime box updates.
- `/.gitignore`: Ignore rules for repository artifacts.

## Working Guidelines for Agents
1. Keep changes minimal and focused on the requested task.
2. Preserve the existing style and language used in `README.md`.
3. When editing generated sections (for example WakaTime blocks), only modify content within clearly marked start/end tags if required.
4. Avoid introducing unnecessary files, dependencies, or workflow changes.
5. Validate markdown formatting after edits.

## Validation Checklist
- Confirm markdown files render correctly.
- Ensure workflow file syntax remains valid when modified.
- Do not commit secrets, tokens, or private data.

## Notes
- This repository is lightweight and primarily documentation/workflow oriented.
- Prefer documentation-safe edits and avoid structural changes unless explicitly requested.
